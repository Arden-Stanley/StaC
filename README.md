# StaC
A Stack-Oriented C style Programming Language Interpreter

---

## How to run

To run a StaC file run the following:

```bash
./StaC.sh <.stac file>
```

If that fails run this:

```bash
chmod +x StaC.sh
```

then run the command above.
